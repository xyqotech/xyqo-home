[phases.setup]
nixPkgs = ["python311", "pip", "gcc", "musl", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
